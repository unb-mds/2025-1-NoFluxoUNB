name: Run web scraper

on:
  push:
    branches: [main]
  workflow_dispatch:
  schedule:
    - cron: '0 0 1 * *'  # Executa às 00:00 do dia 1 de cada mês 

jobs:
  jobScraping:
    runs-on: ubuntu-latest
    env:
      PYTHONUNBUFFERED: "1"
    steps:
      - name: Checkout Repository
        uses: actions/checkout@v3
        
      - name: Setup Python
        uses: actions/setup-python@v4
        with:
          python-version: '3.9'

      - name: Upgrade pip and install Cython
        run: |
          python -m pip install --upgrade pip
          pip install Cython

      - name: Install dependencies
        run: |
          pip install wheel
          pip install --no-build-isolation -r DBA/scraping/requirements.txt

      - name: Bootstrap cursos base file
        run: |
          python - <<'PY'
          import json
          import time
          import urllib.request
          import urllib.error
          import socket
          from pathlib import Path

          urls = [
              "https://dados.unb.br/dataset/cbae3cab-650f-487e-b936-0a5576ff757b/resource/"
              "673b46a0-05f9-4686-9b0c-578c16bc85e0/download/cursos-de-graduacao-08-2024.json",
              "http://dados.unb.br/dataset/cbae3cab-650f-487e-b936-0a5576ff757b/resource/"
              "673b46a0-05f9-4686-9b0c-578c16bc85e0/download/cursos-de-graduacao-08-2024.json",
          ]
          output = Path("DBA/dados/cursos-de-graduacao.json")
          output.parent.mkdir(parents=True, exist_ok=True)

          headers = {"User-Agent": "Mozilla/5.0 (CI bootstrap NoFluxo)"}
          payload = None
          last_error = None

          for url in urls:
              for attempt in range(1, 6):
                  print(f"Tentando download ({attempt}/5): {url}", flush=True)
                  try:
                      req = urllib.request.Request(url, headers=headers)
                      with urllib.request.urlopen(req, timeout=180) as response:
                          payload = json.loads(response.read().decode("utf-8"))
                      print(f"Download concluido: {url} (tentativa {attempt})")
                      break
                  except (urllib.error.URLError, socket.timeout, TimeoutError, ValueError) as exc:
                      last_error = exc
                      wait_s = min(30, attempt * 5)
                      print(
                          f"Falha ao baixar de {url} (tentativa {attempt}/5): {exc}. "
                          f"Aguardando {wait_s}s..."
                      )
                      time.sleep(wait_s)
              if payload is not None:
                  break

          if payload is None:
              raise RuntimeError(
                  f"Nao foi possivel baixar cursos-de-graduacao.json apos retries. "
                  f"Ultimo erro: {last_error}"
              )

          output.write_text(
              json.dumps(payload, ensure_ascii=False, indent=2),
              encoding="utf-8",
          )
          print(f"Arquivo base gerado em: {output}")
          PY

      - name: Create scraping log directory
        run: mkdir -p DBA/logs

      - name: Execute scraping_cursos.py
        working-directory: DBA/scraping
        run: |
          set -o pipefail
          python -u scraping_cursos.py 2>&1 | tee ../logs/scraping_cursos.log

      - name: Execute scraping_ementa.py
        working-directory: DBA/scraping
        run: |
          set -o pipefail
          python -u scraping_ementa.py 2>&1 | tee ../logs/scraping_ementa.log

      - name: Execute scraping_equivalencias.py
        working-directory: DBA/scraping
        run: |
          set -o pipefail
          python -u scraping_equivalencias.py 2>&1 | tee ../logs/scraping_equivalencias.log

      - name: Execute scraping_turmas.py
        working-directory: DBA/scraping
        run: |
          set -o pipefail
          python -u scraping_turmas.py 2>&1 | tee ../logs/scraping_turmas.log

      - name: Upload scraping data
        uses: actions/upload-artifact@v4
        with:
          name: scraping-dados
          path: DBA/dados
          if-no-files-found: error

      - name: Upload scraping logs
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: scraping-logs
          path: DBA/logs/*.log
          if-no-files-found: warn

  jobDBA:
    needs: jobScraping  # <-- Garante que o banco de dados só roda depois do scraping
    runs-on: ubuntu-latest
    env:
      PYTHONUNBUFFERED: "1"
    steps:
      - name: Checkout Repository
        uses: actions/checkout@v3
        
      - name: Setup Python
        uses: actions/setup-python@v4
        with:
          python-version: '3.9'

      - name: Upgrade pip and install Cython
        run: |
          python -m pip install --upgrade pip
          pip install Cython

      - name: Install dependencies
        run: |
          pip install wheel
          pip install --no-build-isolation -r DBA/scraping/requirements.txt

      - name: Download scraping data
        uses: actions/download-artifact@v4
        with:
          name: scraping-dados
          path: DBA/dados

      - name: Create DBA log directory
        run: mkdir -p DBA/logs

      - name: Execute 01_insert_cursos_matrizes_materias.py
        env:
          SUPABASE_URL: ${{ secrets.SUPABASE_URL }}
          SUPABASE_ANON_KEY: ${{ secrets.SUPABASE_ANON_KEY }}
          SUPABASE_SERVICE_ROLE_KEY: ${{ secrets.SUPABASE_SERVICE_ROLE_KEY }}
        run: |
          set -o pipefail
          python -u DBA/database/01_insert_cursos_matrizes_materias.py 2>&1 | tee DBA/logs/01_insert_cursos_matrizes_materias.log

      - name: Execute 02_insert_pre_requisitos_equivalencias.py
        env:
          SUPABASE_URL: ${{ secrets.SUPABASE_URL }}
          SUPABASE_ANON_KEY: ${{ secrets.SUPABASE_ANON_KEY }}
          SUPABASE_SERVICE_ROLE_KEY: ${{ secrets.SUPABASE_SERVICE_ROLE_KEY }}
        run: |
          set -o pipefail
          python -u DBA/database/02_insert_pre_requisitos_equivalencias.py 2>&1 | tee DBA/logs/02_insert_pre_requisitos_equivalencias.log

      - name: Execute 03_insert_turmas.py
        env:
          SUPABASE_URL: ${{ secrets.SUPABASE_URL }}
          SUPABASE_ANON_KEY: ${{ secrets.SUPABASE_ANON_KEY }}
          SUPABASE_SERVICE_ROLE_KEY: ${{ secrets.SUPABASE_SERVICE_ROLE_KEY }}
        run: |
          set -o pipefail
          python -u DBA/database/03_insert_turmas.py 2>&1 | tee DBA/logs/03_insert_turmas.log
          
      - name: Commit Changes
        uses: stefanzweifel/git-auto-commit-action@v4
        with:
          commit_message: "[CI] Relatórios encontrados e baixados com sucesso!"
          file_pattern: "DBA/dados/**/*.json"

      - name: Upload DBA logs
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: dba-logs
          path: DBA/logs/*.log
          if-no-files-found: warn
