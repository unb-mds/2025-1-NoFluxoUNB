// Server hooks removed — all auth is handled client-side.
// See: src/lib/guards/authGuard.ts and src/routes/+layout.svelte
//
// This file is intentionally empty. With adapter-static (SPA mode),
// hooks.server.ts is not executed at runtime.
